# soccer-market-value-prediction

Code is stored in project_data.ipynb. It is set to only use a smaller portion of data currently so that it runs faster. 

Importing from kagglehub may not work therefore use the player_value_named.csv file to get the data.
